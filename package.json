{
  "name": "medicine-ledger",
  "version": "1.0.0",
  "private": true,
  "description": "Medicine stock and dispensing tracker with its own username/email/password login and push notifications — no external services required.",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "web-push": "^3.6.7",
    "pg": "^8.11.5"
  },
  "engines": {
    "node": ">=18"
  }
}
